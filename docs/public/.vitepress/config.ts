/**
 * 📝 VitePress Config Entry Point
 *
 * VitePress procura por config.ts nesta localização por padrão.
 * Este arquivo apenas re-exporta a configuração real de /core/config.ts
 * para manter organização (configurações em /core)
 */

export { default } from './core/config';
