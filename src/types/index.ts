/**
 * TypeScript Types
 *
 * Interfaces, types, enums, etc.
 */

// Base types (will be populated from CbAdmin)
export interface BaseProps {
  /** Component ID */
  id?: string;
  /** CSS class */
  class?: string;
}
